# macOS
.DS_Store

# Local tooling and agent config
.claude/

# Pre-production source archive (kept local, not part of the deployed site)
CravioLabs (1)/

# Editor
.vscode/
.idea/

# Environment
.env
.env.*

# Dependencies and build output (future-proofing)
node_modules/
dist/

# Vercel local project linkage
.vercel/
.vercel

# Internal strategy documents. This repository is PUBLIC, so these must never
# be committed: they contain pricing strategy, compliance analysis and
# commercial positioning that is not for publication.
Kalvano-Website-Revamp-Plan.md
Kalvano-Portfolio-Build-Brief.md
CravioLabs_GoToMarket_Phase3.docx
context/
.agents/
deliverables/

# Third-party reference material, not ours to redistribute
*.pdf

# Local preview scaffolding, regenerated on demand
preview-pending-work.html

# Motion working files (render sources, masters, alternate cuts, score). Production
# deploys from this PUBLIC repo, so only the hero loop the site uses is tracked.
/assets/video/*
!/assets/video/kalvano-hero-square.mp4
!/assets/video/kalvano-hero-square-poster.jpg
